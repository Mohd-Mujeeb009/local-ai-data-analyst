# Optional dependencies for PDF retrieval.
#
# Kept out of requirements.txt on purpose. These pull in torch and roughly a
# gigabyte of model weights on first run, which would triple install time and CI
# duration for a feature that only applies to PDF uploads. Without them the app
# runs normally and answers PDF questions from truncated context instead.
#
#   pip install -r requirements-rag.txt

-r requirements.txt

sentence-transformers==6.0.1
chromadb==1.5.9
rank-bm25==0.2.2
